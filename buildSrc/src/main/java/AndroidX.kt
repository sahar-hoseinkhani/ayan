object AndroidX {
    private const val coreKtxVersion = "1.9.0"
    const val coreKtx = "androidx.core:core-ktx:${coreKtxVersion}"
    private const val lifecycleRuntimeKtxVersion = "2.5.1"
    const val lifecycleRuntimeKtx =
        "androidx.lifecycle:lifecycle-runtime-ktx:${lifecycleRuntimeKtxVersion}"
    private const val appCompatActivityVersion = "1.5.1"
    const val appCompatActivity = "androidx.appcompat:appcompat:${appCompatActivityVersion}"

    private const val androidxAnnotationVersion = "1.5.0"
    const val androidxAnnotation = "androidx.annotation:annotation:${androidxAnnotationVersion}"

    private const val constraintLayoutVersion = "2.1.4"
    const val constraintLayout =
        "androidx.constraintlayout:constraintlayout:$constraintLayoutVersion"

    private const val viewPagerVersion = "1.0.0"
    const val viewPager = "androidx.viewpager2:viewpager2:$viewPagerVersion"

    private const val multiDexVersion = "2.0.1"
    const val multiDex = "androidx.multidex:multidex:$multiDexVersion"

    private const val swipeRefreshLayoutVersion = "1.1.0"
    const val swipeRefreshLayout =
        "androidx.swiperefreshlayout:swiperefreshlayout:$swipeRefreshLayoutVersion"

    private const val browserVersion = "1.4.0"
    const val browser = "androidx.browser:browser:$browserVersion"


}
